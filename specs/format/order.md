Here we define the spec for the datatype `Order`, which represents an order.

This spec uses the datatype `Ticket`, which is defined in [`ticket.md`](
ticket.md).

JSON Format
===========

```js
{
	"tickets": Ticket[],
}
```
